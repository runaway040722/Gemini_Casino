#ifndef HORSERACING_H
#define HORSERACING_H

#include <iostream>
#include <windows.h>
#include "System.h" // ClearBuffer, SetColor 등이 선언된 공통 헤더

// 경마 게임 메인 함수
void PlayHorseRacing(int& money);

// 말들의 정보를 관리하기 위한 구조체 (필요 시 헤더에 선언)
// HorseRacing.cpp 내부에서만 사용한다면 cpp에 두어도 무방합니다.

#endif